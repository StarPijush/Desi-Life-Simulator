import 'package:flutter/material.dart';

class CricketerPage extends StatelessWidget {
  const CricketerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cricketer Page')),
      body: const Center(child: Text('Cricketer Page Placeholder')),
    );
  }
}

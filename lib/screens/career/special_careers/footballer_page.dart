import 'package:flutter/material.dart';

class FootballerPage extends StatelessWidget {
  const FootballerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Footballer Page')),
      body: const Center(child: Text('Footballer Page Placeholder')),
    );
  }
}

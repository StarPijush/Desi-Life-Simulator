import 'package:flutter/material.dart';

const double kPressScale = 0.98;
const Duration kFastDuration = Duration(milliseconds: 80);
const Duration kNormalDuration = Duration(milliseconds: 180);
const Duration kSlowDuration = Duration(milliseconds: 300);
const Curve kTapCurve = Curves.easeOut;
